{
  "name": "user-directory-prototype",
  "private": true,
  "pnpm": { "onlyBuiltDependencies": ["esbuild"] },
  "scripts": {
    "dev": "concurrently \"pnpm --filter shell dev\" \"pnpm --filter directory dev\""
  },
  "devDependencies": { "concurrently": "^8.2.2" }
}
